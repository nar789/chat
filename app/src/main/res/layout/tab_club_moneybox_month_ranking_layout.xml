<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingHorizontal="20dp"
    android:background="@color/gray_25"
    android:paddingBottom="12dp"
    android:paddingTop="20dp"
    android:layout_marginBottom="8dp"
    >
    <TextView
        android:id="@+id/rankingTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        style="@style/Title51622Medium"
        android:textColor="@color/gray_900"
        android:text="@string/a_giving_ranking_of_month"
        />
    <LinearLayout
        android:id="@+id/llShowAll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/rankingTitle"
        app:layout_constraintBottom_toBottomOf="@id/rankingTitle"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Title41824Medium"
            android:textColor="@color/gray_600"
            android:text="@string/j_show_all"
            />
    </LinearLayout>
    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrierTop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:constraint_referenced_ids="rankingTitle,llShowAll"
        app:barrierDirection="bottom"
        />
    <TextView
        android:id="@+id/tvRankDateRange"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/barrierTop"
        android:layout_marginTop="8dp"
        style="@style/Caption11218Regular"
        android:textColor="@color/gray_600"
        android:text="기간 입력하는 곳 "
        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rankList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tvRankDateRange"
        android:layout_marginTop="16dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        android:orientation="vertical"
        tools:listitem="@layout/tab_club_moneybox_month_ranking_list"
        android:nestedScrollingEnabled="false"
        />
</androidx.constraintlayout.widget.ConstraintLayout>