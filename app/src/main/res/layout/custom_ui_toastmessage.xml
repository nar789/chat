<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_popup_toast"
        app:layout_constraintTop_toTopOf="@id/tvToastMessage"
        app:layout_constraintBottom_toBottomOf="@id/tvToastMessage"
        app:layout_constraintStart_toStartOf="@id/tvToastMessage"
        app:layout_constraintEnd_toEndOf="@id/tvToastMessage"
        />
    <TextView
        android:id="@+id/tvToastMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        style="@style/Body21420Regular"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:paddingVertical="16dp"
        android:paddingHorizontal="28dp"
        android:textColor="@color/gray_25"
        android:text=""
        />

</androidx.constraintlayout.widget.ConstraintLayout>